# Write a function  that accepts a size and the text
# of a messafe that should be printed on the shirt

def make_shirt(size, message):
    print(f"Shirt size: {size}, with message: {message}")

make_shirt('medium', 'nike')
make_shirt(message='nike', size='medium')